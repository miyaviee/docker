data:
  image: busybox
  container_name: data
  volumes:
    - "/root"
    - "/home"
    - "/share"
  privileged: true
test:
  build: ruby
  container_name: test
  volumes_from:
    - data
  privileged: true
  restart: always
